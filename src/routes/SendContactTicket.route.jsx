import {APP_NAME} from "../App.config";

function SendContactTicketRoute() {
    document.title = "Send Contact Ticket - " + APP_NAME;

    return (
        <main></main>
    );
}

export default SendContactTicketRoute;