// Local Backend
const LOCAL_BACKEND = {
    type: "LocalBackend",
    endpoint: ""
};

// Backend
const BACKEND = {
    type: "Backend",
    endpoint: "https://someones-backend.onrender.com/"
};

// Current UDC Source
export const UDC_SOURCE = LOCAL_BACKEND;